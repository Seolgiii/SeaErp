# SEAERP — 수산물 재고·원가 ERP

Next.js 15 + Airtable + Vercel. 1인 기획·개발 + Claude Code.

## 이 파일의 쓰기 정책
- 작업 이력·진행률·완성도·측정 수치를 기록하지 않는다.
- 세션 요약·변경 이력은 docs/journal.md에만 쓴다.
- 규칙이 바뀌면 이 파일의 해당 줄을 고친다. 아래에 덧붙이지 않는다.
- Airtable 스키마 변경(테이블·필드 생성/삭제/리네임, 옵션 추가/삭제)은
  사전 승인을 받는다. 필드 삭제는 어떤 경우에도 직접 하지 않는다. 읽기는 자유.

## 문서 라우팅
- UI·화면 작업을 시작하기 전에 DESIGN.md를 읽는다 (WSL: /mnt/c/Users/user/Documents/Obsidian/SEAERP/DESIGN.md).
- 업무 흐름·도메인 개념이 필요하면 docs/업무프로세스.md를 읽는다.
- 미해결 버그·위험은 docs/RISKS.md에 있다. 작업 영역이 거기 걸리는지 먼저 본다.
- 중장기 목표·단계 계획이 필요하면 docs/ROADMAP.md를 읽는다.
- 스타일 정리 진행 상황은 STYLE-MIGRATION.md에 있다.
- Airtable 스키마(테이블·필드·옵션)는 문서로 남기지 않는다. MCP로 실물을 조회한다.
- docs/journal.md는 이력 보관용이다. 읽지 않는다.

## 개발 원칙
- 작업 범위 밖의 코드는 손대지 않는다. "이왕 손대는 김에"를 거부한다. 잘 작동 중인 코드를 우선 보호하고, 리팩토링이 필요하면 별도 작업으로 분리해 제안한다.
  (왜: LOT 재고·결재·회계 도메인이라 의도하지 않은 변경의 대가가 크다 / 2026-05)
- 변경마다 검증 사각지대를 함께 밝힌다 — 테스트되지 않은 부분 / 검증되지 않은 가정 / 도그푸딩으로도 안 잡힐 곳.
  (왜: 사용자가 비개발자라 생성된 코드를 직접 감사할 수 없다. 테스트는 백엔드에 몰려 있고 UI는 손 도그푸딩이다 / 2026-06-11)
- 작업을 시작하기 전에 테스트가 그린인지 확인한다. 깨진 채 시작하면 그 사실을 먼저 보고한다.
  (왜: 깨진 테스트를 물려받으면 내 변경이 원인인지 구분할 수 없다 / 2026-08-04)
- 데이터 정합성을 기능보다 우선한다. 검증을 새로 넣을 때는 차단이 아니라 모니터링(로그)부터 시작한다.
- 보안 변경은 backward compatible을 우선한다.
- 정합성 위험 지점은 [INTEGRITY-ALERT], 스키마 불일치는 [SCHEMA-MISMATCH] prefix로 로깅한다.
- Airtable 필드명은 추측하지 않는다. 실물로 확인한다.
- alert를 쓰지 않는다. toast / 인라인 에러 / 모달로 메시지 무게에 맞게 나눈다.
- 버전은 package.json이 단일 출처다. PATCH=버그·소수정 / MINOR=기능·화면 추가 / MAJOR=정식 출시. 눈에 보이는 변화가 배포될 때만 1회 올린다. docs·리팩토링·WIP은 올리지 않는다.

## 아키텍처
- 하나의 Next.js 프로젝트가 두 PWA를 낸다. 모바일=작업자(WORKER), PC=관리자(ADMIN·MASTER, /admin 하위).
- 백엔드(app/actions/**, lib/**)는 양쪽이 공유한다. 새 기능은 모바일용인지 PC용인지 양쪽인지 밝히고 시작한다.
- Airtable 접근은 lib/airtable.ts 헬퍼로 통일한다. 컴포넌트·라우트에서 직접 fetch하지 않는다.
- 원가·단가·손익은 /admin에서만 노출한다. 모바일·작업자 화면에는 넣지 않는다.
  (왜: 관리회계 정보이고 현장 작업자에게 열 정보가 아니다 / 2026-06-01)
- 거래 이력은 읽기전용 원장이다. 등록·수정 기능을 여기 넣지 않는다(재고 작업 쪽으로 분리한다).

## 권한·보안
- 서버 액션은 클라이언트를 믿지 않고 권한을 재검증한다 — requireWorker / requireAdmin / requireMaster (lib/server-auth.ts). role은 Airtable에서 직접 조회한다.
- /admin의 WORKER 예외 허용은 lib/admin-access.ts 하나로만 판단한다. 화면마다 따로 분기하지 않는다.
- 관리 API는 ADMIN_SECRET을 강제한다(lib/admin-auth.ts). 디버그·프리뷰 API는 production에서 차단한다.
- 지출 100만원 이상의 즉시 승인은 MASTER만 할 수 있다. 서버에서 재검증한다.
- POST 라우트는 X-Idempotency-Key로 5분간 중복을 막는다.
- 출고 승인 멱등 가드는 "출고시점 단가"로 판정한다. 판매원가를 기준으로 쓰지 않는다.
  (왜: 판매원가는 반려 시 보존되므로 가드로 쓰면 재승인이 영구 차단된다 / 2026-05-18)
- PIN은 scrypt 해시로 저장한다. 평문 PIN이 매칭되면 즉시 해시로 바꾼다.
- PIN 실패 5회는 5분 잠금, 풀린 뒤 다시 5회는 30분 잠금. Airtable에 영속화한다.
- 세션은 30분 idle로 만료한다. 5분 남으면 연장 배너를 띄운다.
- 입력은 제어문자 제거 + 필드별 길이 제한으로 sanitize한다.

## 데이터 정합성
- 반려는 레코드를 지우지 않는다. 수량 0 + 승인상태 반려로 남긴다.
- 입고 반려 시 보관처 비용 4필드(냉장료단가·입출고비·노조비·동결비)를 모두 null로 되돌리고 LOT 재고수량을 0으로 둔다.
  (왜: 입고가 취소되면 그에 부과된 작업비도 함께 취소된다 / 2026-08-04)
- 출고 반려 시 잔여수량·LOT 재고수량을 복구하고 출고시점 비용 6필드(단가·냉장료·입출고비·노조비·동결비·손익)를 null로 한다. "출고시점 판매원가"는 지우지 않고 보존한다.
  (왜: 반려돼도 "이 원가로 시도했다"는 cost basis 지표로 남길 값이다 / 2026-05-18)
- 반려 후 재승인은 자동 복원한다.
- 이동 반려는 안전 가드 3종을 모두 통과할 때만 자동 복구한다 — 신규 LOT 재고가 이동수량과 같을 것 / 신규 LOT을 원본으로 한 재이동이 없을 것 / 신규 입고관리에서 나간 출고가 없을 것. 하나라도 실패하면 [INTEGRITY-ALERT]를 남기고 반려 처리 자체를 차단한다(수동 보정으로 넘긴다). 정의는 app/actions/inventory/transfer.ts, 호출은 app/actions/admin/admin.ts.
  (왜: 후속 거래가 얹힌 이동을 되돌리면 재고가 모순 상태로 들어간다 / 2026-05-14)
- LOT 일련번호는 낙관적 재시도로 보호한다. 검증~POST 사이 race window가 남아 있다.
- Airtable 응답 zod 검증은 모니터링 모드다. 실패해도 흐름을 막지 않고 로그만 남긴다.

## 원가
보관처 비용은 성격이 둘로 갈린다.
- 냉장료: 보관 기간에 비례해 계속 발생
- 입출고비·노조비·동결비: 작업이 실제 수행될 때 1회 발생

재고 이동 시에는 실제로 다시 수행되는 작업만 부과한다.
동결은 이미 언 물건에 재수행되지 않으므로 이동 LOT의 동결비는 0으로 둔다.
원본의 동결비 cost basis는 박스당 값으로 이월 보존한다(N단계 이동에도 총액 보존).
  (동결비는 최초 입고 1회 작업비 / 2026-08-04 확인)

## LOT
- LOT번호 = YYMMDD-품목코드-규격-미수-일련번호. 영업일은 KST 오전 9시로 끊는다(9시 이전은 전날).
- 미수가 비면 그 자리를 통째로 생략한다. 규격이 비면 `-`, 품목코드가 비면 `NOCODE`를 넣는다.
- 수량은 박스 단위로만 다룬다. qtyBase/qtyDetail 같은 단위 분기를 새로 만들지 않는다.
  (왜: 단위가 둘이면 원가 계산이 통째로 이원화된다 / 2026-05-13)
- 이동으로 만드는 새 LOT은 최초입고일을 원본에서 그대로 물려받는다. 누적 경비는 이동입고일이 있으면 그 날짜를, 없으면 최초입고일을 기준으로 계산한다.
  (왜: 이동 때마다 보관일수가 리셋되면 장기 재고가 숨는다 / 2026-05-13)
- LOT 상태·상태사유 문자열은 lib/status.ts 상수로만 비교한다. 리터럴을 직접 쓰지 않는다.
  (왜: 공백 유무 차이로 조건이 영영 거짓이 되는 사고가 난다 / 2026-07-28)

## 재고 사건 — LOT을 만들거나 줄이는 경로
- 입고 승인 → LOT 생성 + 보관처 비용 적용
- 출고 승인 → 입고관리 잔여수량·LOT 재고수량 차감 + 출고시점 비용 7필드 스냅샷
- 이동 승인 → 새 입고관리·LOT 생성, 원본 차감
- 가공 완료 → 투입 원물 차감, 가공품 LOT 생성. 입고·출고·이동과 나란한 네 번째 사건이며 성격은 변환이다.
- 작업 정산 확정 → 생산내역 1줄이 입고관리 1행 + LOT 1개를 만든다
- 경로를 새로 추가하면 이 목록을 함께 갱신한다.
- 입출고증 PDF는 자사창고가 끝점일 때만 발행한다(lib/storage.ts:isOwnStorage). 외부창고끼리의 이동에는 발행하지 않는다.
  (왜: 우리가 발행 주체가 아닌 거래에 우리 증빙을 낼 수 없다 / 2026-05-19)

## UI
- UI 작업 전 DESIGN.md를 읽고 그 토큰을 따른다. 색·폰트·간격·radius를 임의로 지정하지 않는다. 토큰에 없는 값이 필요하면 만들지 말고 보고한다.
- 목록·표 화면 규칙(구조·넘침 처리·숫자 표기·정렬 방향·공통 컴포넌트 강제)은 DESIGN.md가 정본이다. 이 파일에 옮겨 적지 않는다. 표 규칙은 /admin PC 화면 전용이며 모바일 작업자 화면에 적용하지 않는다.
- 목록·검색 화면의 필터 상태는 URL 쿼리에 동기화한다. 검색 입력은 300ms 디바운스한다.
- 모바일 하위 화면은 자체 헤더를 만들지 않고 `components/PageHeader.tsx`를 쓴다.
  (왜: 뒤로가기·제목·우측 슬롯 구조가 화면마다 갈리면 작업자가 매번 다시 익힌다 / 2026-05)
- Modal은 포털하지 않는다.
  (왜: 포털하면 admin 서브트리를 벗어나 모바일 토큰을 받는다 / 2026-07-28)
- LOT 상세 진입은 LOT번호 링크로 통일한다(app/admin/_lot-link.tsx). 행 전체 클릭을 새로 만들지 않는다.
- 스타일 하드코딩 정리는 일괄 스윕하지 않는다. 다른 작업으로 연 파일만 정리하고 별도 커밋으로 분리한다. 적용 대상·제외 대상·중단 임계는 STYLE-MIGRATION.md가 정본이며, 진행 상황도 거기에 기록한다.

## 코드 위치
- app/actions/ — 서버 액션 (admin / inventory / expense / my-requests / dashboard)
- app/admin/ — PC 관리자 화면 · app/inventory·expense·my-requests — 모바일 화면
- lib/ 주요 파일
  - Airtable: airtable.ts(헬퍼) · airtable-schema.ts(테이블·필드 상수) · schemas/(zod)
  - 권한·세션: server-auth.ts · admin-auth.ts · admin-access.ts · session.ts · pin-hash.ts · pin-rate-limit{,-core}.ts
  - 원가: cost-calc.ts(출고시점 비용·손익) · storage-cost.ts(보관처 비용 이력) · work-settlement-rates.ts · processing-rate-basis.ts
  - LOT·재고: lot-sequence.ts(일련번호 동시성) · lot-detail.ts · lot-inventory.ts · stock-deduction.ts · status.ts · storage.ts(isOwnStorage)
  - 결재: approval-service.ts · approval-types.ts · idempotency.ts · bulk-submit.ts
  - 표시·입력: number-format.ts · spec-display.ts · format-datetime.ts · input-sanitize.ts
  - 운영: daily-report.ts(일일 정산 메일) · resend.ts · logger.ts · version.ts
  - 봇: eohwang/ (어황일보 사진 → 일본어 번역, Telegram 웹훅)

## 운영
- Vercel 자동 배포. 프로덕션 도메인은 seafood-erp.vercel.app이다(seaerp 아님).
- 일일 정산 메일은 매일 09:00 KST cron으로 나간다(vercel.json).
- production 콘솔 출력은 lib/logger.ts로 막는다.
- 환경변수는 .env.example을 기준으로 본다.

## 비범위
- 재무회계(분개·전표·부가세·전자세금계산서·결산)는 만들지 않는다. 관리회계(원가·손익)까지만 자체 구현하고 나머지는 외부 회계 프로그램으로 넘긴다.
  (왜: 세법 변경이 잦고 세무 리스크가 크다. 1인 개발로 감당할 범위가 아니다 / 2026-05-28)
